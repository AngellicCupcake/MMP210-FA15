

function setup() {
    //create the Canvas
    createCanvas(windowWidth, windowHeight);  
}


function draw(){
    fill(70, 27, 126);
    stroke(230, 187, 255);
    strokeWeight(5);
    var rectWidth = 450;
    rect(windowWidth/2 - rectWidth/2, 50, 450, 650);
    
}